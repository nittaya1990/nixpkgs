--- wine-3.1/libs/wine/config.c	2018-02-02 10:21:44.000000000 -0800
+++ wine-3.1-preloader-disabled/libs/wine/config.c	2018-02-06 15:32:12.481794752 -0800
@@ -548,7 +548,7 @@

     if (!name) name = argv0_name;  /* no name means default loader */

-#ifdef linux
+#if defined(linux) && !defined(__i386__)
     use_preloader = !strendswith( name, "wineserver" );
 #else
     use_preloader = 0;
